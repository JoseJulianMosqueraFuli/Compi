# Enforce the packageManager declared in package.json.
engine-strict=true
# Defense in depth: lifecycle scripts only run for packages explicitly
# allow-listed in `package.json#pnpm.onlyBuiltDependencies`. Adding a new
# native dep requires editing that list on purpose.
